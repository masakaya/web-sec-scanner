# Prefect ワークフローオーケストレーション

このプロジェクトでは、ワークフローオーケストレーションツールとして **Prefect** を使用しています。

## 📋 概要

Prefectは、Pythonでデータパイプラインやワークフローを構築、スケジュール、監視するための最新のワークフローオーケストレーションツールです。

### 主な特徴

- ✅ **Pythonネイティブ**: デコレータベースのシンプルなAPI
- ✅ **観測性**: リアルタイムでワークフローの実行を監視
- ✅ **エラーハンドリング**: 自動リトライ、タイムアウト、エラー通知
- ✅ **スケジューリング**: cron形式やinterval形式でのスケジュール実行
- ✅ **ダッシュボード**: Webベースの管理UIで実行履歴を確認

---

## 🚀 クイックスタート

### Prefectサーバーの起動

```bash
# Prefectサーバーを起動（別ターミナルで実行）
poe prefect-server
```

サーバーが起動したら、ブラウザで http://127.0.0.1:4200 にアクセスしてダッシュボードを確認できます。

### サンプルフローの実行

```bash
# サンプルフローを実行
poe prefect-flow
```

---

## 📚 基本的な使い方

### フローとタスクの定義

Prefectでは、`@flow` と `@task` デコレータを使ってワークフローを定義します。

```python
from prefect import flow, task

@task
def my_task(value: int) -> int:
    """タスクの定義"""
    return value * 2

@flow
def my_flow(numbers: list[int]) -> list[int]:
    """フローの定義"""
    results = []
    for num in numbers:
        result = my_task(num)
        results.append(result)
    return results

# 実行
if __name__ == "__main__":
    my_flow([1, 2, 3, 4, 5])
```

### タスクのオプション

タスクには様々なオプションを設定できます：

```python
@task(
    name="custom-task-name",
    description="タスクの説明",
    retries=3,              # リトライ回数
    retry_delay_seconds=60, # リトライ間隔（秒）
    timeout_seconds=300,    # タイムアウト（秒）
)
def my_task():
    pass
```

### ログ出力

Prefectのロガーを使用してログを出力できます：

```python
from prefect import task
from prefect.logging import get_run_logger

@task
def log_example():
    logger = get_run_logger()
    logger.info("情報ログ")
    logger.warning("警告ログ")
    logger.error("エラーログ")
```

---

## 🔧 プロジェクト内のフロー

### サンプルフロー

`flows/example_flow.py` にサンプルフローが用意されています：

```python
from flows.example_flow import example_workflow

# フローの実行
result = example_workflow(
    name="Your Name",
    numbers=[10, 20, 30]
)
```

---

## 🛠️ 利用可能なコマンド

### Prefect関連タスク

```bash
# Prefectサーバー起動
poe prefect-server

# サンプルフロー実行
poe prefect-flow

# フローのデプロイ
poe prefect-deploy
```

### その他のPrefect CLIコマンド

```bash
# フロー一覧表示
prefect flow ls

# デプロイメント一覧
prefect deployment ls

# ワーカーの起動
prefect worker start --pool default

# 実行履歴の確認
prefect flow-run ls
```

---

## 📖 スケジュール実行

フローをスケジュール実行するには、デプロイメントを作成します：

### デプロイメント定義ファイル

`prefect.yaml` ファイルを作成してデプロイメントを定義：

```yaml
deployments:
  - name: example-deployment
    entrypoint: flows/example_flow.py:example_workflow
    schedule:
      cron: "0 9 * * *"  # 毎日9時に実行
    work_pool:
      name: default
```

### デプロイメントの作成

```bash
# デプロイメント作成
prefect deploy --all

# ワーカー起動（デプロイメント実行用）
prefect worker start --pool default
```

---

## 🔍 ダッシュボードの使い方

Prefect UIダッシュボード (http://127.0.0.1:4200) では以下を確認できます：

- **Flow Runs**: フロー実行履歴とステータス
- **Deployments**: デプロイメント一覧とスケジュール
- **Work Pools**: ワーカープール管理
- **Blocks**: 設定やシークレットの管理

---

## 💡 ベストプラクティス

### 1. タスクの粒度

タスクは適切な粒度で分割しましょう：

```python
# ❌ 粒度が大きすぎる
@task
def do_everything():
    fetch_data()
    transform_data()
    save_data()

# ✅ 適切な粒度
@task
def fetch_data():
    pass

@task
def transform_data():
    pass

@task
def save_data():
    pass
```

### 2. エラーハンドリング

重要なタスクには適切なリトライ設定を：

```python
@task(retries=3, retry_delay_seconds=60)
def api_call():
    """外部API呼び出しなど、失敗する可能性があるタスク"""
    pass
```

### 3. ログの活用

適切なログを出力して、実行状況を追跡可能に：

```python
@task
def process_data(data):
    logger = get_run_logger()
    logger.info(f"Processing {len(data)} items")
    # 処理...
    logger.info("Processing completed")
```

### 4. パラメータ化

フローはパラメータ化して再利用可能に：

```python
@flow
def etl_flow(source: str, target: str, batch_size: int = 100):
    """パラメータで動作を変更可能なフロー"""
    pass
```

---

## 📚 参考リンク

- [Prefect公式ドキュメント](https://docs.prefect.io/)
- [Prefect GitHub](https://github.com/PrefectHQ/prefect)
- [Prefect Concepts](https://docs.prefect.io/latest/concepts/)
- [Prefect Recipes](https://docs.prefect.io/latest/recipes/)

---

## 🐛 トラブルシューティング

### サーバーが起動しない

```bash
# Prefectのデータベースをリセット
prefect server database reset -y
```

### フローが実行されない

```bash
# ワーカーが起動しているか確認
prefect worker ls

# デプロイメントが作成されているか確認
prefect deployment ls
```

### ログが表示されない

`get_run_logger()` を使用してPrefectのロガーからログを出力してください。標準の `print()` や `logging` モジュールは推奨されません。
